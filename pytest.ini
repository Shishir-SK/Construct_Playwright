[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --alluredir=allure-results
# Playwright options (can override via CLI):
# pytest --headed --slowmo 500
# pytest --browser webkit
